# docker compose -f docker-compose.translation.yml run --rm trans
services:
  trans:
    restart: "no"
    build:
      target: translations
      context: ./
    env_file:
      - .env.example
    volumes: 
      - ./:/app