version: "3.4"

# docker-compose -f docker-compose.translation run trans

services:
  trans:
    restart: "no"
    build:
      context: ./
      dockerfile: Dockerfile.translation
    env_file:
      - .env.example
    volumes: 
      - ./:/app